# Python
__pycache__/
*.py[cod]
*.pyo
.env
.venv/
venv/
*.egg-info/

# Data — raw input only (large files, regenerated locally)
backend/data/raw/
backend/data/processed/load_5min_clean.csv
backend/data/processed/features_hourly.csv
# load_hourly.csv IS committed — Render needs it for lag features (1.7MB)

# Models — small model (0.4MB) is committed to git for Render
# Only exclude large files
backend/models/training_report.txt

# Weather data — INCLUDED in git (850 KB, needed for build)
# backend/data/weather/  <-- intentionally NOT excluded

# Node
frontend/node_modules/
frontend/dist/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
